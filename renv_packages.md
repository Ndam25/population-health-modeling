# R Environment — renv.lock (abbreviated)
# Full lock file generated by: renv::snapshot()
# Restore with: renv::restore()
#
# Key packages:
#   survey      >= 4.2    Complex survey estimation (DHS/MICS)
#   mice        >= 3.16   Multiple imputation
#   lme4        >= 1.1    Mixed-effects models
#   survival    >= 3.5    Survival analysis
#   forecast    >= 8.21   ARIMA forecasting
#   bsts        >= 0.9    Bayesian structural time-series
#   deSolve     >= 1.35   ODE solvers (compartmental models)
#   tidyverse   >= 2.0    Data manipulation
#   ggplot2     >= 3.4    Visualization
#   purrr       >= 1.0    Functional programming
#   readr       >= 2.1    Data I/O
#   broom       >= 1.0    Model tidying
#   spdep       >= 1.2    Spatial statistics
#   sensitivity >= 1.28   Sensitivity analysis (PSA)
#
# R version: 4.3.x
# Platform: x86_64-pc-linux-gnu / x86_64-apple-darwin
